//+------------------------------------------------------------------+
//|Custom utils function library                                     |
//+------------------------------------------------------------------+

#ifndef _UTILS
#define _UTILS

uint max(const int a, const int b){
   if (a > b) return a;
   else return b;
}

uint min(const uint a, const uint b){
   if (a < b) return a;
   else return b;
}

#endif 